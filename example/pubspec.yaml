name: arbify_counter_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

dev_dependencies:
  arbify:
    path: ../
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


arbify:
  url: http://localhost:8000
  project_id: 4
